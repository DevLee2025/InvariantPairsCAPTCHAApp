# OS junk
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.venv/

# ML pipeline artifacts (regenerable — see pipeline/README.md)
# data/: downloaded PACS + computed embeddings/probs; out/: HF export outputs
pipeline/data/
pipeline/out/

# Multi-annotator server storage (shared games + per-annotator comments)
server/data/

# Per-machine Claude Code permissions
.claude/settings.local.json

# IRB administrative documents (ignored from git tracking)
irb/


# NOTE: web/.gitignore excludes web/node_modules, web/dist, and the ~148 MB
# web/public/pacs images + real manifest (rebuilt by pipeline/build_real_manifest.py).
# pipeline/OTGTests/ and Archive/ ARE tracked on purpose: human-collected game
# data is primary data and not regenerable.
